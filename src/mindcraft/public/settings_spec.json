{
    "profile": {
        "type": "object",
        "required": true,
        "description": "The profile object to use, including name, prompts, and examples"
    },
    "minecraft_version": {
        "type": "string",
        "description": "The version of Minecraft to use. Set to 'auto' to automatically detect the version.",
        "default": "auto"
    },
    "host": {
        "type": "string",
        "description": "The minecraft server host address to connect to",
        "default": "127.0.0.1"
    },
    "port": {
        "type": "number",
        "description": "The minecraft server port to connect to. -1 for auto-detect.",
        "default": 55916
    },
    "auth": {
        "type": "string",
        "description": "The authentication method to use",
        "default": "offline",
        "options": ["offline", "microsoft"]
    },
    "base_profile": {
        "type": "string",
        "description": "Allowed values: survival, assistant, creative, god_mode. Each has fine tuned settings for different game modes.",
        "default": "survival",
        "options": ["survival", "assistant", "creative", "god_mode"]
    },
    "load_memory": {
        "type": "boolean",
        "description": "Whether to load bot's previous memory",
        "default": false
    },
    "init_message": {
        "type": "string",
        "description": "The initial message to send to the bot",
        "default": "Respond with hello world and your name"
    },
    "only_chat_with": {
        "type": "array",
        "description": "List of agents to only chat with. If empty, the bot will chat publicly",
        "default": []
    },
    "speak": {
        "type": "boolean",
        "description": "Whether to enable text-to-speech reading on the host machine",
        "default": false
    },
    "language": {
        "type": "string",
        "description": "The language to automatically translate to and from using google translate",
        "default": "en"
    },
    "allow_vision": {
        "type": "boolean",
        "description": "Whether to allow vision capabilities",
        "default": false
    },
    "blocked_actions": {
        "type": "array",
        "description": "List of actions that are blocked",
        "default": ["checkBlueprint", "checkBlueprintLevel", "getBlueprint", "getBlueprintLevel"]
    },
    "relevant_docs_count": {
        "type": "number",
        "description": "Number of relevant function documents to include in the prompt for LLM code writing",
        "default": 5
    },
    "max_messages": {
        "type": "number",
        "description": "Maximum number of recent messages to keep in context for LLM",
        "default": 15
    },
    "num_examples": {
        "type": "number",
        "description": "Number of examples to select to help prompt better LLM responses",
        "default": 2
    },
    "max_commands": {
        "type": "number",
        "description": "Maximum number of commands allowed in consecutive responses. -1 for no limit",
        "default": -1
    },
    "narrate_behavior": {
        "type": "boolean",
        "description": "Whether to openly chat automatic behavior like 'Picking up item!'",
        "default": true
    },
    "log_all_prompts": {
        "type": "boolean",
        "description": "Whether to log all prompts to file. Can be very verbose.",
        "default": false
    },
    "show_command_syntax": {
        "type": "string",
        "description": "Whether to show \"full\" command syntax, \"shortened\" command syntax, or \"none\"",
        "default": "full",
        "options": ["full", "shortened", "none"]
    },
    "chat_ingame": {
        "type": "boolean",
        "description": "Whether to show bot's chat messages in game chat",
        "default": true
    },
    "chat_bot_messages": {
        "type": "boolean",
        "description": "Whether to publicly chat messages to and from other bots",
        "default": true
    },
    "render_bot_view": {
        "type": "boolean",
        "description": "Whether to render bot view for user observation. Does not give bot vision.",
        "default": false
    },
    "allow_insecure_coding": {
        "type": "boolean",
        "description": "Whether to allow newAction command that let's LLM write/run code on host computer. Despite sandboxxing, it is potentially insecure.",
        "default": false
    },
    "use_brain_agent": {
        "type": "boolean",
        "description": "Enable the planner/task agent system.",
        "default": true
    },
    "agent_state_enabled": {
        "type": "boolean",
        "description": "Persist goals, recent memory and active task checkpoints across restarts.",
        "default": true
    },
    "resume_active_task": {
        "type": "boolean",
        "description": "Resume a running task from its latest checkpoint after restart.",
        "default": true
    },
    "persistent_memory_max_events": {
        "type": "number",
        "description": "Maximum number of recent persistent memory events.",
        "default": 120
    },
    "persistent_memory_context_events": {
        "type": "number",
        "description": "Number of recent persistent memories included in planning prompts.",
        "default": 12
    },
    "task_checkpoint_messages": {
        "type": "number",
        "description": "Number of active task messages retained for recovery.",
        "default": 30
    },
    "task_max_steps": {
        "type": "number",
        "description": "Maximum model/tool iterations in one bounded task.",
        "default": 50
    },
    "autonomy_continue_delay_ms": {
        "type": "number",
        "description": "Delay before the planner chooses the next autonomous subtask.",
        "default": 3000
    },
    "autonomy_max_consecutive_failures": {
        "type": "number",
        "description": "Pause autonomous work after this many failed subtasks.",
        "default": 5
    },
    "progress_chat": {
        "type": "boolean",
        "description": "Show throttled public plan and tool status in Minecraft chat.",
        "default": true
    },
    "progress_chat_show_tools": {
        "type": "boolean",
        "description": "Show tool actions in Minecraft progress messages.",
        "default": true
    },
    "progress_chat_show_plans": {
        "type": "boolean",
        "description": "Show model-generated player-safe plan summaries.",
        "default": true
    },
    "progress_chat_show_reasoning": {
        "type": "boolean",
        "description": "Show concise structured reasoning summaries in Minecraft chat. Raw provider reasoning is never sent to chat.",
        "default": true
    },
    "progress_chat_min_interval_ms": {
        "type": "number",
        "description": "Minimum time between progress chat messages.",
        "default": 2500
    },
    "progress_chat_interval_ms": {
        "type": "number",
        "description": "Interval for a status update while a model request is still running.",
        "default": 12000
    },
    "progress_chat_max_length": {
        "type": "number",
        "description": "Maximum length of one Minecraft progress message.",
        "default": 200
    },
    "log_model_reasoning": {
        "type": "boolean",
        "description": "Log raw model-provider reasoning to the server console. This can be verbose and may include sensitive context.",
        "default": false
    },
    "log_model_reasoning_max_chars": {
        "type": "number",
        "description": "Maximum provider-reasoning characters logged per model response.",
        "default": 12000
    },
    "code_timeout_mins": {
        "type": "number",
        "description": "Number of minutes to allow code execution. -1 for no timeout",
        "default": -1
    },
    "task": {
        "type": "object",
        "description": "The task object to give the agent on start. If null, the agent will not have a task.",
        "default": null
    },
    "spawn_timeout": {
        "type": "number",
        "description": "Number of seconds allowed for the bot to spawn before throwing an error. Increase when spawning takes a while.",
        "default": 30
    }
}
